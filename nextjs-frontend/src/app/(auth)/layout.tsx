// Auth pages placeholder
// Full implementation: Story 1.3 (User Registration & Routing)
// Will contain: Login page, Signup page backed by Supabase Auth
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
